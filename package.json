{
  "name": "cozycave-mern",
  "version": "1.0.0",
  "description": "CozyCave MERN application runner",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
